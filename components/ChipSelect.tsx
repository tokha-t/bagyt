"use client";
import {useRef} from 'react';
export default function ChipSelect<T extends string>({options,value,mode,onChange,ariaLabel}:{options:{value:T;label:string}[];value:T|T[]|null;mode:'single'|'multi';onChange:(v:T|T[])=>void;ariaLabel:string}){const root=useRef<HTMLDivElement>(null);return <div ref={root} className="chips" role={mode==='single'?'radiogroup':'group'} aria-label={ariaLabel}>{options.map((o,i)=>{const checked=Array.isArray(value)?value.includes(o.value):value===o.value;return <button type="button" key={o.value} role={mode==='single'?'radio':'checkbox'} aria-checked={checked} className={checked?'chip active':'chip'} onKeyDown={e=>{if(['ArrowRight','ArrowDown','ArrowLeft','ArrowUp'].includes(e.key)){e.preventDefault();const j=(i+(['ArrowRight','ArrowDown'].includes(e.key)?1:-1)+options.length)%options.length;root.current?.querySelectorAll('button')[j]?.focus();if(mode==='single')onChange(options[j].value);}}} onClick={()=>onChange(mode==='single'?o.value:checked?(Array.isArray(value)?value:[]).filter(v=>v!==o.value):[...(Array.isArray(value)?value:[]),o.value])}>{o.label}</button>;})}</div>;}
